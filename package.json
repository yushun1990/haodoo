{
  "name": "haodoo",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "patch:foliate": "node scripts/patch-foliate.mjs && node scripts/patch-foliate-legacy.mjs",
    "postinstall": "npm run patch:foliate",
    "sync:classic": "tsx scripts/sync-classic.ts",
    "predev": "npm run patch:foliate && npm run sync:classic",
    "dev": "vite",
    "prebuild": "npm run patch:foliate && npm run sync:classic",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src scripts vite.config.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "preview": "vite preview",
    "test:reader-smoke": "node scripts/smoke-reader.mjs"
  },
  "dependencies": {
    "foliate-js": "1.0.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "csv-parse": "^6.1.0",
    "oxlint": "^1.51.0",
    "playwright-core": "^1.54.2",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "typescript": "~6.0.2",
    "vite": "^8.0.0",
    "zod": "^4.1.5"
  }
}
